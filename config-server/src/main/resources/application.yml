server:
  port: 8083

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:beludominguez/examen-esp-backend-dominguez-lia-belen.git
          search-paths: config-server
          default-label: master
          force-pull: false
