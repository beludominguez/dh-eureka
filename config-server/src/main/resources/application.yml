server:
  port: 8080

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:/home/jpressacco/Documents/EspecializacionBack/examen-esp-backend-dominguez-lia-belen
          #uri: ssh:git@github.com:beludominguez/dh-eureka.git
          search-paths: config-server
          default-label: master
          force-pull: false
