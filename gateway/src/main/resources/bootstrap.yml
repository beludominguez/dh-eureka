spring:
  application:
    name: gateway
  profiles:
    active: development
  cloud:
    gateway:
      routes:
        - id: catalog
          uri: lb://CATALOG-SERVICE
          predicates:
            - Path=/catalog/**
      discovery:
        locator:
          enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8888/eureka}